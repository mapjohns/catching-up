export default function manageComments(state = { comments: [] }, action) {
    switch(action.type) {
        case "ADD_COMMENT":
        debugger
        
        default:
            return state
    }
}
